{
  "name": "phoenix-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "build": "vite build",
    "test": "echo 'finishing the mission without touching other files.' && node --check index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.3.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  }
}